import { SettingsPage } from "@/features/settings/components/SettingsPage";

export const metadata = {
  title: "설정 | 헤쳐모여",
  description: "계정 및 앱 환경설정",
};

export default function SettingsRoute() {
  return <SettingsPage />;
}
